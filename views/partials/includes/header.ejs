<header>
    <a href="/home" class="site-logo mb-2"><span>CMU E-Course</span></a>
    <% if(cart){ %>
        <a href="path/to/shopping/cart" id="btn-cart" class="btn btn-primary" aria-label="View 3 items in your shopping cart">
            <i class="fa fa-shopping-cart" aria-hidden="true"></i>
        </a>
    <% } %>
    <% if(signUp){ %>
        <a href=# class="sign-in">Sign In/Sign Up</a>
    <% } %>
</header>